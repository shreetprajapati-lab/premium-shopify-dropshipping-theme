document.addEventListener('DOMContentLoaded',()=>{const h=document.querySelector('#SiteHeader'),t=document.querySelector('#Toast');const toast=s=>{if(!t)return;t.textContent=s;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)};addEventListener('scroll',()=>h&&h.classList.toggle('scrolled',scrollY>24),{passive:true});const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');o.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>o.observe(e));document.querySelectorAll('.quick-add-form').forEach(f=>f.addEventListener('submit',async e=>{e.preventDefault();const b=f.querySelector('button');b.disabled=true;try{const r=await fetch(f.action,{method:'POST',headers:{Accept:'application/json'},body:new FormData(f)});if(!r.ok)throw 0;const c=await (await fetch('/cart.js')).json();document.querySelectorAll('.cart-count').forEach(x=>x.textContent=c.item_count);toast('Added to cart');}catch(x){toast('Could not add this item');}b.disabled=false}))});