document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('#SiteHeader'),toastEl=document.querySelector('#Toast'),dialog=document.querySelector('#QuickviewDialog'),qContent=document.querySelector('#QuickviewContent');const toast=s=>{if(!toastEl)return;toastEl.textContent=s;toastEl.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>toastEl.classList.remove('show'),2200)};addEventListener('scroll',()=>header&&header.classList.toggle('scrolled',scrollY>24),{passive:true});const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));document.querySelectorAll('.quick-add-form').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const b=form.querySelector('button');b.disabled=true;try{const r=await fetch(form.action,{method:'POST',headers:{Accept:'application/json'},body:new FormData(form)});if(!r.ok)throw new Error();const cart=await (await fetch('/cart.js')).json();document.querySelectorAll('.cart-count').forEach(x=>x.textContent=cart.item_count);toast('Added to cart ✓')}catch(err){toast('Could not add this item')}finally{b.disabled=false}}));document.querySelectorAll('[data-gallery-image]').forEach(b=>b.addEventListener('click',()=>{const img=document.querySelector('#ProductImage');if(img)img.src=b.dataset.galleryImage}));document.querySelectorAll('[data-wishlist]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();b.textContent=b.textContent==='♡'?'♥':'♡';toast(b.textContent==='♥'?'Saved to wishlist':'Removed from wishlist')}));document.querySelectorAll('[data-quick-view]').forEach(b=>b.addEventListener('click',async()=>{try{const r=await fetch(b.dataset.productUrl+'.js');const p=await r.json();qContent.innerHTML=`<div style="display:grid;grid-template-columns:1fr 1fr;gap:28px;align-items:center"><img src="${p.featured_image}" alt="${p.title}" style="width:100%;border-radius:16px;background:#f4f5f3"><div><div class="eyebrow">Quick view</div><h2>${p.title}</h2><p class="product-price">${(p.price/100).toLocaleString('en-IN',{style:'currency',currency:'INR'})}</p><a class="btn btn-primary" href="${b.dataset.productUrl}">View product →</a></div></div>`;dialog.showModal()}catch(e){window.location.href=b.dataset.productUrl}}));document.querySelectorAll('[data-quickview-close]').forEach(b=>b.addEventListener('click',()=>dialog.close()));document.querySelectorAll('[data-sort]').forEach(s=>s.addEventListener('change',()=>{const u=new URL(location.href);u.searchParams.set('sort_by',s.value);location.href=u.toString()}));document.querySelectorAll('[data-cart-quantity]').forEach(i=>i.addEventListener('change',async()=>{await fetch('/cart/change.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:i.dataset.cartQuantity,quantity:Number(i.value)})});location.reload()}));const h=document.querySelector('[data-hours]'),m=document.querySelector('[data-minutes]'),sec=document.querySelector('[data-seconds]');if(h&&m&&sec){let left=12*3600+34*60+56;setInterval(()=>{left=Math.max(0,left-1);h.textContent=String(Math.floor(left/3600)).padStart(2,'0');m.textContent=String(Math.floor(left%3600/60)).padStart(2,'0');sec.textContent=String(left%60).padStart(2,'0')},1000)}});